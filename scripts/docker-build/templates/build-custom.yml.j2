---

# Container preparation
- hosts: containers
  gather_facts: true
  become: yes
  vars:
    # packages
    package_mode: upgrade-verbose
    # rootca
    rootca_certificates: []
  roles:
    - deitkrachten.rootca
    - deitkrachten.package
