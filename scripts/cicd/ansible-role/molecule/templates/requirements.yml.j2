---

roles:
<% set org = generic.org %>
<% set roles = (molecule.requirements.roles + (molecule.requirements.roles_extra | default([]))) | unique %>
<% for role in roles %>
<% set src = 'https://github.com/de-it-krachten/ansible-role-' + (role.name | default(role) | regex_replace(org + '.')) + '.git' %>
  - name: <= (role.name|default(role)) =>
    src: <= role.src if role.src is defined else src =>
    version: <= role.version | default('main') =>
    scm: git
<% endfor %>
