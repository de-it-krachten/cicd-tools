---

# Container preparation
- hosts: containers
  gather_facts: true
  become: yes
  vars:
    upgrade: true
  roles:
    - { role: deitkrachten.package, package_mode: upgrade-verbose, when: upgrade|bool }
